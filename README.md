This script works with the current interface of the uber eats page (date: 01/05/2026).
Because playwright reads the DOM, any major changes that might happen will deprecate the script.
<br>
In case that happens, the user can always change the get_x functions so that it matches the new DOM. 
Chat gpt is good at reading html. Use devtools in the page to find the new structure and give it to chat so user can update the sript to future needs.


## THIS IS A PROTOTYPE
<br>Future implementations:
<br>-> Anti bot detection
<br>-> JSON scanning instead of UI scanning
<br>-> basic EDA functions
